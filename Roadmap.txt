Milestone 0 — Project setup (foundation)
0.1 Initialize repo + tooling

 Create Next.js TS app (App Router) + basic scripts
DoD: pnpm dev (or npm/yarn) runs; TypeScript builds; repo has lint/format scripts.

0.2 Add Tailwind + shadcn/ui baseline layout

 Install Tailwind, configure globals

 Add layout shell: header + sidebar + main content
DoD: App renders a consistent layout across pages; at least 1 shadcn component used.

0.3 Add Postgres (local) + Prisma setup

 Add Docker compose for Postgres

 Install Prisma, create schema, run first migration
DoD: prisma migrate dev succeeds; DATABASE_URL works locally.

0.4 Auth (Auth.js / NextAuth) + protected routes

 Add auth provider (Credentials or GitHub)

 Protect dashboard route group
DoD: Unauthed users get redirected; authed users see dashboard.

0.5 Seed script (minimal data)

 Create scripts/seed.ts to insert 2 equipment + 2 technicians
DoD: Running seed results in rows in DB; app lists them (even if basic).

Milestone 0 DoD: Fresh clone → env set → docker up → migrate → seed → login → see dashboard.

Milestone 1 — Core CRUD (Equipment, Technicians, Task Templates)
1.1 Equipment model + migration

 Add Equipment table (name, asset_tag, location, category, criticality, status)
DoD: Migration applied; Prisma client can create/find equipment.

1.2 Equipment list page

 Page shows table of equipment with search input (client-side ok)
DoD: Seeded equipment displays; empty state shown if none.

1.3 Equipment create form

 Create page with form + Zod validation
DoD: Invalid inputs show errors; valid submit creates row and redirects to list.

1.4 Equipment edit + retire

 Edit page; “Retire” sets status=retired (no delete in UI)
DoD: Edit persists; retired equipment is visually labeled and filtered toggle exists.

1.5 Technician model + migration

 Add Technician (name, email, skills text/json, team, active)
DoD: Migration applied; seeded technicians show in DB.

1.6 Technician CRUD pages

 List + create + edit
DoD: Basic CRUD works; active/inactive displayed and editable.

1.7 Task Template model + migration

 TaskTemplate (title, description, expected_minutes, frequency_days, equipment_id optional)
DoD: Migration applied; can create templates linked/unlinked to equipment.

1.8 Task Template CRUD pages

 List + create + edit, filter by equipment (optional)
DoD: expected_minutes validated; templates display with equipment association.

Milestone 1 DoD: You can manage equipment, technicians, and task templates end-to-end in UI.

Milestone 2 — Work Orders + lifecycle
2.1 WorkOrder model + migration

 WorkOrder fields: equipment_id, task_template_id nullable, status, priority, due_date

 Add expected_minutes_snapshot
DoD: Migration applied; Prisma create/read works.

2.2 Create Work Order (manual)

 Create page to create ad-hoc work order (no template)
DoD: Work order created; appears in list; expected_minutes_snapshot can be entered.

2.3 Create Work Order from Template

 Create flow that selects template and copies expected_minutes → snapshot
DoD: Snapshot equals template value at creation; shows both template + snapshot.

2.4 Work Order list + filters

 List view with filters: status, priority, due date range (basic)
DoD: Filtering works; empty state; pagination optional.

2.5 Work Order detail page

 Detail shows equipment, template (if any), status, priority, due date
DoD: Direct link to detail works; info is readable and consistent.

2.6 Status transitions (planned → in_progress → done)

 Buttons or dropdown to update status
DoD: Status updates persist; invalid transitions blocked (simple rule ok).

Milestone 2 DoD: You can create work orders (template/ad-hoc), view them, and move through statuses.

Milestone 3 — Scheduling + planned downtime
3.1 Add scheduling fields + migration

 WorkOrder: scheduled_start, scheduled_end
DoD: Migration applied; fields visible on detail page (even if empty).

3.2 Add planned downtime fields + validation

 planned_downtime boolean; downtime_start/end

 Zod rules: end > start; downtime within scheduled window (recommended)
DoD: Invalid schedules are blocked with clear errors.

3.3 Scheduling UI on work order

 Add schedule section on detail page
DoD: User can set scheduled times + downtime; saves and re-renders correctly.

3.4 Basic schedule view

 “Schedule” page showing work orders grouped by day/week (table ok)
DoD: Scheduled work orders appear in correct day buckets.

3.5 (Optional) Equipment downtime conflict check

 Prevent overlapping downtime for same equipment (soft warning ok)
DoD: When overlap detected, user sees warning; optionally blocks save.

Milestone 3 DoD: You can schedule work orders, model downtime windows, and view the upcoming schedule.

Milestone 4 — Work logs (actuals)
4.1 WorkLog model + migration

 WorkLog: work_order_id, technician_id, start_time, end_time, notes, actual_minutes
DoD: Migration applied; derived actual_minutes computed on write.

4.2 Add work log create form

 Create log from work order detail page
DoD: Valid log saved; actual_minutes computed; appears in logs list on detail.

4.3 Edit/delete work log (optional delete)

 Allow edit; optional soft-delete
DoD: Edit persists; actual_minutes recomputed; audit note optional.

4.4 “Done requires logs” rule

 Block marking work order done without at least 1 log
DoD: Attempt shows message; adding log then allows completion.

4.5 “My Work” view (technician)

 Page shows scheduled/in-progress work orders (initially all, later assigned)
DoD: Technician can find a work order quickly and add logs.

Milestone 4 DoD: Actual execution time is recorded reliably and gates completion.

Milestone 5 — Shifts + staffing capacity
5.1 Shift model + migration

 Shift: site/location, start_time, end_time
DoD: Migration applied; shift rows creatable.

5.2 Shift CRUD pages

 List + create + edit
DoD: Shifts are viewable; validation ensures end > start.

5.3 ShiftAssignment model + migration

 ShiftAssignment: shift_id, technician_id
DoD: Can assign multiple technicians to shift; unique constraint prevents duplicates.

5.4 Shift staffing UI

 Shift detail page: assign/unassign technicians
DoD: Staffing list updates live; shows count.

5.5 Work orders mapped to shifts (time overlap)

 For a shift, compute which work orders overlap shift time window
DoD: Shift detail displays overlapping scheduled work orders.

5.6 Capacity vs workload summary (v1)

 Compute planned workload minutes in shift (sum expected_minutes_snapshot of overlapping work orders with planned_downtime)

 Compute staffed tech minutes = (# techs) * shift duration minutes
DoD: Shift page shows “workload vs capacity” numbers and a simple status (OK / Risk).

Milestone 5 DoD: Shifts show staffing, downtime-related workload, and a clear capacity comparison.

Milestone 6 — Analytics dashboards
6.1 Task duration aggregation query

 Compute avg actual_minutes by task template (or by task title if ad-hoc)
DoD: Query returns correct results; matches sample logs.

6.2 Percentiles (p50/p90) for task durations

 Implement p50/p90 (DB query or application calc)
DoD: Dashboard displays median + p90 for tasks with enough samples (e.g., >=5 logs).

6.3 Overrun rate metric

 Overrun rate where actual > expected * 1.2
DoD: Dashboard table shows overrun % per template; handles divide-by-zero safely.

6.4 Technician workload dashboard (neutral framing)

 Minutes logged per technician per week + count of work orders
DoD: Filter by date range; totals match raw logs.

6.5 Downtime + staffing analytics (shift level)

 List shifts with workload vs capacity and risk indicator
DoD: “Understaffed shifts” table renders; links to shift detail.

6.6 Basic charting

 Add 1–2 charts (Recharts): e.g., bar chart of avg duration by task
DoD: Charts render with real data and have readable axes/labels.

Milestone 6 DoD: Analytics pages tell a clear story: duration variance, overruns, workload distribution, and staffing risk.

Milestone 7 — Production polish (pick 6–10 tickets)
7.1 RBAC roles + enforcement

 Roles: Admin / Planner / Technician stored in DB/user session
DoD: Unauthorized actions are blocked server-side; UI hides restricted actions.

7.2 Audit log for critical changes

 Record status/schedule/downtime changes (who/what/when)
DoD: Audit entries appear on work order detail; includes timestamp + actor.

7.3 CSV export

 Export work orders/logs/analytics table as CSV
DoD: Download works; CSV columns correct; respects filters.

7.4 Notifications (optional)

 Email reminder for upcoming scheduled work orders (cron)
DoD: Local dev can trigger job; sends email via test provider/log output.

7.5 CI pipeline

 GitHub Actions: lint + unit tests + build
DoD: PR checks run; failing tests block merge.

7.6 E2E smoke test

 Playwright: create equipment → template → work order → schedule → log → analytics visible
DoD: Test runs in CI; stable selectors; no flaky timing.

7.7 Performance & pagination

 Add pagination to work order list + basic indexes
DoD: List remains fast with 1k rows; explain in README.

7.8 Error handling + toasts

 Consistent error boundary + toast notifications for mutations
DoD: Failures show actionable message; no silent failures.

Milestone 7 DoD: App feels professional: permissions, auditability, exports/tests/CI, and deploy readiness.